/**
 * Memberships page locators
 */

export const membershipsLocators = {
    managerTab: (page) => page.getByRole("link", { name: "Manager" }),
    membershipsTab: (page) => page.getByRole("link", { name: "memberships-icon Memberships" }),
    addMembershipButton: (page) => page.getByRole("button", { name: "Add Membership" }),
    nameInput: (page) => page.getByRole("textbox", { name: "Name of membership" }),
    creditMembershipButton: (page) => page.locator('button[name="new-credit-membership"]'),
    serviceMembershipButton: (page) => page.locator('button[name="new-service-membership"]'),
    selectServiceButton: (page) => page.getByRole("button", { name: "Select a service" }),
    searchForAServicePlaceholder: (page) => page.getByPlaceholder("Search for a service"),
    numberOfServicesPlaceholder: (page) => page.getByPlaceholder("Unlimited"),
    recurringFeeInput: (page) => page.getByRole("textbox", { name: "Recurring fee Open currency" }),
    signupFeeInput: (page) => page.getByRole("textbox", { name: "Signup fee Open currency" }),
    valueOfBenefitInput: (page) => page.getByRole("textbox", { name: "Value of benefit Open" }),
    discountAfterBenefitInput: (page) => page.getByRole("textbox", { name: "Discount after benefit Open" }),
    productDiscountInput: (page) => page.getByRole("textbox", { name: "Product discount Open" }),
    okButton: (page) => page.getByRole("button", { name: "Ok" }),
    saveButton: (page) => page.getByRole("button", { name: "Save changes" }),
    saveMembershipButton: (page) => page.locator('button[name="save-membership"]'),
    newBenefitButton: (page) => page.getByRole("button", { name: "New Benefit" }),
    benefitsButton: (page) => page.locator('button[name="step-next"]'),
    creditButton: (page) => page.locator("div.bg-white", { hasText: "Credit" }),
    recurringServiceButton: (page) => page.locator("div.bg-white", { hasText: "Recurring Service" }),
    discountButton: (page) => page.locator("div.bg-white", { hasText: "Service Discount" }),
    discountOpenPercentage: (page) => page.getByRole("textbox", { name: "Discount Open percentage" }),
    cancelOnlineSwitch: (page) => page.locator('div[name="cancellable-online"] button[role="switch"]'),
    buyOnlineSwitch: (page) => page.locator('div[name="available-online"] button[role="switch"]'),
    backButton: (page) => page.getByRole("button", { name: "Back" }),
    addBenefitButton: (page) => page.getByRole("button", { name: "Add Benefit" }),
    creditAmountInput: (page) => page.getByRole("textbox", { name: "Credit Amount" }),
    calculator: (page) => page.getByRole("textbox", { name: "0" }),
    closeButton: (page) => page.getByRole("button", { name: "Close" }),
    onlineButton: (page) => page.locator('button[name="step-next"]:has-text("Online")'),
    termsButton: (page) => page.locator('button[name="step-next"]:has-text("Terms")'),
    selectedServicesButton: (page) => page.getByRole("radio", { name: "SELECTED_SERVICES" }),
    selectServicesButton: (page) => page.getByRole("button", { name: "Select services" }),
    selectAllButton: (page) => page.getByRole("button", { name: "Select all", exact: true }),
    doneButton: (page) => page.getByRole("button", { name: "Done" }),
    recurringFeeTaxDropdown: (page) => page.getByRole("button", { name: "No Tax Rate", exact: true }),
    signupFeeTaxDropdown: (page) => page.getByLabel("Signup fee tax"),
    beautyVatOption: (page) => page.getByText("Beauty VAT"),
    hairVatOption: (page) => page.locator("//label[normalize-space(.)='Signup fee tax']/following::ul[@role='listbox'][1]//span[normalize-space(.)='Hair VAT']"),
    membershipRow: (page, membershipName) => page.locator("tr", { hasText: membershipName }),
    membershipCheckbox: (page, membershipName) => page.locator("tr", { hasText: membershipName }).locator('button[role="checkbox"]'),
    archiveButton: (page) => page.getByRole("button", { name: "Archive" }),
    yesButton: (page) => page.getByRole("button", { name: "Yes" }),
    okButtonConfirm: (page) => page.getByRole("button", { name: "OK" }),
    billingLink: (page) => page.getByRole("link", { name: "Billing" }),
    billingFailedLink: (page) => page.getByRole("link", { name: "Billing failed" }),
    billingDueLink: (page) => page.getByRole("link", { name: "Billing due" }),
    membershipCheckboxByName: (page, membershipName) => page.locator(`//tr[td//div[contains(normalize-space(), '${membershipName}')]]//button[@role='checkbox']`),
    moveToBillingDueButton: (page) => page.getByRole("button", { name: "Move to Billing Due Tab" }),
    manuallyBilledButton: (page) => page.getByRole("button", { name: "Manually Billed" }),
    discardModal: (page) => page.locator("h2", { hasText: "Discard all unsaved changes" }),
    discardChangesButton: (page) => page.getByRole("button", { name: "Discard changes" }),
    serviceNameButton: (page, serviceName) => page.getByRole("button", { name: serviceName })
};
